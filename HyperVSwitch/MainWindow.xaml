<Window x:Class="HyperVSwitch.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Hyper-V 切换助手" Height="480" Width="560"
        ResizeMode="CanMinimize"
        WindowStartupLocation="CenterScreen"
        Background="#F0F2F5">

    <Window.Resources>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Height" Value="44"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Border Grid.Row="0" Style="{StaticResource CardStyle}" Padding="20,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Ellipse Width="40" Height="40" VerticalAlignment="Center"
                         Fill="{Binding StatusColor}" Margin="0,0,14,0">
                    <Ellipse.Effect>
                        <DropShadowEffect BlurRadius="8" ShadowDepth="0" Opacity="0.3"/>
                    </Ellipse.Effect>
                </Ellipse>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="{Binding StatusTitle}" FontSize="20" FontWeight="Bold"
                               Foreground="#1A1A2E"/>
                    <TextBlock Text="{Binding StatusDescription}" FontSize="13"
                               Foreground="#666" Margin="0,4,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 详情信息 -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="20">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>

                    <!-- 模式标签 -->
                    <Border CornerRadius="8" Padding="14" Margin="0,0,0,16"
                            Background="{Binding ModeGradientBrush}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="当前模式" FontSize="12" Foreground="#FFFFFFCC" />
                                <TextBlock Text="{Binding CurrentMode}" FontSize="28" FontWeight="Bold"
                                           Foreground="White" Margin="0,2,0,0"/>
                            </StackPanel>
                            <TextBlock Grid.Column="1" VerticalAlignment="Center"
                                       FontSize="56" Text="{Binding ModeEmoji}"/>
                        </Grid>
                    </Border>

                    <!-- 兼容性详情 -->
                    <TextBlock Text="兼容性状态" FontSize="14" FontWeight="SemiBold"
                               Foreground="#333" Margin="0,0,0,8"/>

                    <TextBlock Text="{Binding DetailInfo}" FontSize="13"
                               Foreground="#555" LineHeight="22" TextWrapping="Wrap"
                               Margin="4,0,0,0"/>

                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- 底部操作区 -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="20">
            <StackPanel>

                <!-- 需要重启的提示 -->
                <Border x:Name="RebootBanner"
                        Background="#FFF3E0" CornerRadius="8"
                        Padding="12" Margin="0,0,0,12"
                        Visibility="{Binding ShowRebootBanner, Converter={StaticResource BoolToVis}}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⚠️" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <StackPanel>
                            <TextBlock Text="需要重启电脑才能生效" FontSize="13" FontWeight="SemiBold"
                                       Foreground="#E65100"/>
                            <TextBlock Text="已成功更改 Hyper-V 配置，重启后新设置将生效"
                                       FontSize="12" Foreground="#BF360C" Margin="0,2,0,0"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- 按钮区域 -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0"
                            Content="{Binding ToggleButtonText}"
                            Click="ToggleButton_Click">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
                                <Setter Property="Background" Value="{Binding ToggleButtonColor}"/>
                                <Style.Triggers>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Opacity" Value="0.6"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Grid.Column="2"
                            Content="🔄 立即重启"
                            x:Name="RestartButton"
                            Click="RestartButton_Click">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
                                <Setter Property="Background" Value="#4CAF50"/>
                                <Setter Property="Visibility" Value="{Binding ShowRebootBanner, Converter={StaticResource BoolToVis}}"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#388E3C"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>

            </StackPanel>
        </Border>

    </Grid>
</Window>
